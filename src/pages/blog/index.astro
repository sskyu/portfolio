---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { getCollection } from 'astro:content';

const posts = (await getCollection('blog')).sort(
	(a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf()
);
---

<BaseLayout title="Blog | sskyu" description="sskyuの技術ブログ一覧。Web開発、プログラミング、個人開発に関する記事をまとめています。">
	<h1>Blog</h1>
	<ul>
		{posts.map((post) => (
			<li>
				<a href={`/blog/${post.slug}`}>{post.data.title}</a>
				<span class="publish-date">({post.data.pubDate.toLocaleDateString('ja-JP', { year: 'numeric', month: '2-digit', day: '2-digit' })})</span>
			</li>
		))}
	</ul>
</BaseLayout>
